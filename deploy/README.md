# Артефакты выкладки

- **`nginx-dierchat.example.conf`** — пример reverse proxy: HTTPS, `/api`, `/ws`, `/media`, статика SPA.
- Полный чеклист: **[`../HOSTING.md`](../HOSTING.md)** (сервер, Docker, клиенты, firewall).
